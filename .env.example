# Optional: the source video URL, read by scripts/01_download_audio.sh if you
# don't want to pass it as a CLI argument. No API keys are needed anywhere in
# this pipeline -- yt-dlp and Whisper both run keyless.
SOURCE_VIDEO_URL=https://www.youtube.com/watch?v=VIDEO_ID
